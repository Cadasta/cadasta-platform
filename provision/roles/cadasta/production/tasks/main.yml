- name: Install npm-run
  become: yes
  become_user: root
  command: npm install -g npm-run

- name: Build Webpack bundle
  become: yes
  become_user: "{{ app_user }}"
  command: npm-run webpack -p
           chdir="{{ application_path }}app"
